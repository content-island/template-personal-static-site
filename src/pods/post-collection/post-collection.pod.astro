---
import PostCard from './components/post-card.astro';
import type { Post } from './api';

interface Props {
  posts: Post[];
}
const { posts } = Astro.props;
---

<section class="grid max-w-[900px] flex-1 gap-8 xl:grid-cols-2">
  {posts.map(post => <PostCard post={post} />)}
</section>
